// import { createTheme } from "@mui/material/styles";

const globalTheme = {
  typography: {
    fontFamily: [
      "system-ui",
      "roboto",
      "arial",
      "sans-serif"
    ].join(",")
  }
}

export default globalTheme;